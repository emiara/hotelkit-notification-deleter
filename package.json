{
  "scripts": {
    "build": "tsc",
    "publish": "npm run build && zip -r -o ../hotelkit-notification-deleter.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.277",
    "typescript": "^5.6.2"
  }
}
